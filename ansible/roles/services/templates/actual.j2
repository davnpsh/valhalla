version: '3'

services:

  actual_server:
    container_name: actual
    image: docker.io/actualbudget/actual-server:latest
    ports:
      - '{{ valhalla.ip.private }}:5006:5006'
    volumes:
      - ./actual-data:/data
    restart: unless-stopped